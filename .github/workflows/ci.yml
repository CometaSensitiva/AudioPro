name: CI

on:
  push:
    branches:
      - main
      - refactor-quality-hardening
  pull_request:

jobs:
  macos-build-and-test:
    # macos-latest è ancora macOS 15: il deployment target 26 richiede
    # l'immagine macos-26 (arm64, GA).
    runs-on: macos-26

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Run AudioPro test suite
        run: |
          xcodebuild test \
            -scheme AudioPro \
            -project AudioPro.xcodeproj \
            -destination 'platform=macOS' \
            -derivedDataPath .derivedData/ci-test \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO

      - name: Verify AudioPro Release build
        run: |
          xcodebuild build \
            -scheme AudioPro \
            -project AudioPro.xcodeproj \
            -configuration Release \
            -destination 'platform=macOS' \
            -derivedDataPath .derivedData/ci-release \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO
